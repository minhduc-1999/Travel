import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {

    },
    screenTitile: {
      fontSize: 30,
      margin: 30,
      marginLeft: 20,
      fontWeight: 'bold',
    },
})

export default styles;