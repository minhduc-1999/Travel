import {Pressable, StatusBar, Animated} from 'react-native';

export const AnimatedStatusBar = Animated.createAnimatedComponent(StatusBar);
export const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
